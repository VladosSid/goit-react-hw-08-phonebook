# Структура репозиторія

- App;
  - SharedLayout (outlet);
    - Nav;
      - Home(/);
      - Contacts(/contacts);
        - listContacts(ul);
          - contacts(contactsItem)(li);
      - Register(/register);
        - formRegister(form);
      - Login(/login);
        - formLogin(form);
      - User(/userMenu)

## Создание репозитория по шаблону
